# Site Fixtures

Place site-specific fixture extensions here when default shared fixtures are insufficient.

Examples:
- site-specific authenticated storage states
- helper fixtures for custom content types or workflows
